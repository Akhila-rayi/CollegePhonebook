<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.coign.collegephonebook">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>


    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ViewdetailsActivity">
        </activity>
        <activity android:name=".ViewlistActivity" android:theme="@style/AppThemeNoactionbar">
        </activity>
        <activity android:name=".HomeActivity" android:theme="@style/AppThemeNoactionbar">
        </activity>
        <activity android:name=".RegisterActivity" android:theme="@style/AppThemeNoactionbar">
        </activity>
        <activity android:name=".Splashscreen" android:theme="@style/AppThemeNoactionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/AppThemeNoactionbar">
        </activity>
    </application>

</manifest>